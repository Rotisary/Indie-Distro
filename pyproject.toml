[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["F401"]

[tool.isort]
profile = "black"
line_length = 88
sections = ["FUTURE", "STDLIB", "DJANGO", "THIRDPARTY", "FIRSTPARTY", "LOCALFOLDER"]
known_django = ["django"]
known_first_party = ["core", "config"]
